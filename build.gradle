apply plugin: 'idea'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'jars')
    testCompile 'junit:junit:4.12'
}

jar {
    exclude('com/onepagecrm/samples/**')
}
